# Core
fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9
pydantic==2.7.4
pydantic-settings==2.3.4

# Database
sqlalchemy==2.0.31
psycopg2-binary==2.9.9
alembic==1.13.2

# Auth & Security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.1.3
slowapi==0.1.9

# AI / OCR / CV
easyocr==1.7.1
opencv-python-headless==4.10.0.84
numpy==1.26.4
Pillow==10.4.0
scikit-image==0.24.0
# torch and torchvision are installed separately in the Dockerfile using the
# PyTorch CPU-only index (https://download.pytorch.org/whl/cpu). Installing
# them from the default PyPI index here would pull the full CUDA/GPU build,
# which drags in several GB of unnecessary NVIDIA driver packages that a
# CPU-only laptop deployment doesn't need and that make the build extremely
# slow. Keep these commented out — do not uncomment.
# torch==2.3.1
# torchvision==0.18.1

# PDF report generation
reportlab==4.2.0

# Utilities
python-dotenv==1.0.1
email-validator==2.2.0
